# Changelog — SMI-V Plus

บันทึกการเปลี่ยนแปลงของระบบ SMI-V Plus (static site บน GitHub Pages) เรียงจากล่าสุดไปเก่าสุด
ดู commit history เต็มได้ที่ `git log` — ไฟล์นี้สรุปเฉพาะการเปลี่ยนแปลงที่มีผลต่อผู้ใช้งาน (ไม่รวม publish ข้อมูลรายวันที่เกิดจากการใช้งานปกติ)

## Testing & CI

- เพิ่ม regression test suite (`npm test`, Node built-in test runner) ครอบคลุมสูตรคำนวณหลักทั้งหมด (E, I, G, ก่อความรุนแรงซ้ำ, กรองอายุ/ปีงบ), ตรรกะแจ้งเตือนความเสี่ยง/E ต่ำ, ความถูกต้องของไฟล์ Excel ที่ export — 28 เคส
- เพิ่ม GitHub Actions CI รัน `npm test` อัตโนมัติทุกครั้งที่แก้ `docs/app.js`/`docs/ui.js`/`test/**`

## ความปลอดภัยในการเผยแพร่ข้อมูล (Publish pipeline)

- ย้ายจากการฝัง GitHub token ในโค้ด (ถูก GitHub push-protection บล็อก) ไปใช้ Cloudflare Worker เป็นตัวกลาง — เก็บ token จริงไว้ฝั่งเซิร์ฟเวอร์ ไม่เคยอยู่ในเบราว์เซอร์/ซอร์สโค้ดที่เผยแพร่ ผู้ใช้กดปุ่มเดียวเผยแพร่ได้ทันที
- อัปโหลด Excel → เผยแพร่เข้า GitHub อัตโนมัติ (ไม่ต้องกดปุ่มแยก)
- เพิ่ม cooldown 8 วินาที + กันกดซ้อนระหว่างกำลังเผยแพร่ (กันยิง GitHub API ถี่เกินไป)
- เพิ่มการเตือนก่อนเผยแพร่ถ้าจำนวนผู้ป่วยลดลงผิดปกติ (>20%) — กันไฟล์ผิด/ไม่ครบทับข้อมูลจริงโดยไม่รู้ตัว
- แก้บั๊ก sha-conflict (409) จาก GitHub API เมื่อมีการเผยแพร่ซ้อนกัน — retry อัตโนมัติ

## ประวัติการนำเข้า (Import history)

- ทุกครั้งที่เผยแพร่ ระบบเก็บสำเนาข้อมูล ณ เวลานั้นไว้ดูย้อนหลังได้ (อ่านอย่างเดียว) และกู้คืนกลับเป็นข้อมูลปัจจุบันได้
- เพิ่มปุ่มลบรายการประวัติที่ผิดพลาด (เช่น publish ทดสอบเผลอ)
- แก้บั๊กจริง: การตัดประวัติเก่า (keep last N) เดิมไม่เคยลบไฟล์ snapshot จริง ทำให้ repo บวมสะสมไม่จำกัด — ตอนนี้ลบไฟล์จริงเมื่อหลุดออกจากประวัติ และลดจำนวนที่เก็บจาก 30 เหลือ 15
- ผู้ใช้สิทธิ์ดูอย่างเดียว (viewer) เห็นประวัติได้ (ดูอย่างเดียว) แต่กู้คืน/ลบไม่ได้

## สิทธิ์การเข้าถึง (Access control)

- เพิ่มด่านรหัสผ่าน 2 ระดับ: admin (แก้ไข/เผยแพร่/ลบได้ทุกอย่าง) และ viewer (ดูอย่างเดียว) — obscurity เท่านั้น ไม่ใช่ security จริง (ตั้งใจ)
- เพิ่ม throttle กันคนกดรหัสผ่านมั่วซ้ำๆ (ล็อก 5 นาทีหลังผิด 3 ครั้ง)
- เพิ่มปุ่มออกจากระบบ

## Dashboard & การวิเคราะห์

- ตารางรายชื่อผู้ป่วยที่ต้องติดตาม พร้อมระดับความสำคัญ (สูง/กลาง/ปกติ), จำนวนวันค้างติดตาม, ค้นหา/กรอง/จัดเรียงได้
- แบนเนอร์แจ้งเตือนผู้ป่วยความสำคัญสูง (ก่อความรุนแรงซ้ำ) บนสุดของ dashboard
- แจ้งเตือนถ้า E ต่ำกว่าเป้า 40% ต่อเนื่องเกิน 30 วัน
- แจ้งเตือนผ่านเบราว์เซอร์ (Desktop Notification) และปุ่มส่งอีเมลแจ้งเตือน (mailto)
- การ์ดสรุปคุณภาพข้อมูลรวมทั้งจังหวัด, การ์ดเทียบตัวชี้วัดกับปีงบก่อนหน้า
- กราฟแนวโน้ม E(%) ย้อนหลัง, แนวโน้มผู้ป่วยใหม่รายอำเภอข้ามปีงบ
- คัดลอกข้อมูลประชากรจากปีงบก่อนหน้าได้ (ไม่ต้องกรอกใหม่ทุกปี), กันเผลอบันทึกประชากรทับเป็น 0
- URL แชร์มุมมองที่เลือกอยู่ได้ (`?fy=..&level=..&area=..`), จำมุมมองล่าสุดที่ใช้ไว้อัตโนมัติ
- คีย์ลัด `/` โฟกัสช่องค้นหา, `Esc` ปิด panel
- ปีงบประมาณเป็น dropdown (แสดงเฉพาะปีที่มีข้อมูลจริง) แทนช่องกรอกตัวเลข
- ดึงข้อมูลใหม่อัตโนมัติทุก 5 นาที (เหมาะกับเปิดจอค้างไว้ในห้องทำงาน)

## Export & Print

- ส่งออกรายงาน/รายชื่อปัญหาเป็น Excel (แยกราย อำเภอ/หน่วยบริการ/จังหวัด)
- ปุ่มดาวน์โหลดภาพ (PNG) ทุกกราฟอัตโนมัติ — แก้บั๊กจริง: เดิมใช้ `data:` URI ทำให้เบราว์เซอร์มือถือ/webview บางตัวค้าง เปลี่ยนไปใช้ Blob แทน
- พิมพ์สรุปผู้บริหาร 1 หน้า + ดาวน์โหลดเป็น PDF จริง (ไม่ใช่แค่ print dialog)
- แก้ layout การพิมพ์ (กราฟตกขอบ/ถูกตัด, การ์ดถูกแบ่งคาบหน้ากระดาษ)

## เนื้อหาอ้างอิง (คู่มือรหัส)

- สูตรคำนวณ E/L/O อย่างเป็นทางการ, ความชุกโรคแยกราย 4 กลุ่ม, ช่องทางติดต่อกรมสุขภาพจิต (จากเอกสาร template ทางการ)
- สัญญาณเตือนสำหรับญาติ/ชุมชน (SMI-V SCAN 5 ข้อ + 7 สัญญาณเตือนก่อความรุนแรง), การจำแนกระดับความเสี่ยงของแอป V-Care (เกณฑ์สี), หลัก "2 ย 1 ส"
- ชื่อจังหวัดเต็ม (ไม่ใช่แค่รหัส), หัวตารางเต็มไม่ย่อ

## Performance & ความเสถียร

- PWA: ติดตั้งเป็นแอปได้, ดูรายงานล่าสุดได้แม้ออฟไลน์ (service worker cache)
- Cache-busting สำหรับ `app.js`/`ui.js`/`style.css` กันเบราว์เซอร์ผู้ใช้ค้างโค้ดเก่าหลัง deploy
- ปรับ layout ให้ใช้งานบนมือถือ/แท็บเล็ตได้สะดวก
- ปรับความสวยงามทั่วระบบ (hover state, accent bar, focus ring, sticky table header)

## จุดเริ่มต้น

- เริ่มจากระบบ PHP + PostgreSQL (Docker, deploy บน Render) แล้วย้ายมาเป็น static site บน GitHub Pages ทั้งหมด (ไม่มี server/ฐานข้อมูล) เพื่อไม่ต้องเปิดเครื่อง/เซิร์ฟเวอร์ทิ้งไว้ตลอดเวลา
